name: "Run Test"
on:
  workflow_dispatch:

jobs:
  RUN-TEST:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pyver: ["9", "10", "11", "12", "13", "14"]
    steps:
    - name: Checkout
      uses: actions/checkout@main
      with:
        fetch-tags: true
    - name: Install nix
      uses: cachix/install-nix-action@master
    - name: Setup cachix
      uses: cachix/cachix-action@master
      with:
        name: antares0982
    - name: Test Release Build
      run: |
        nix develop -c python ./dev_tools/automated_test.py --build-only --pyver ${{ matrix.pyver }} --build-type Release
    - name: Print Version
      run: |
        PYTHONPATH=./build-py3.${{ matrix.pyver }} .nix-pyenv/bin/python3.${{ matrix.pyver }} -c "import pyyjson;print(pyyjson.__version__)"
